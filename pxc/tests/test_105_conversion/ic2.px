public namespace ic2;
import common -;
import operator -;

struct foo {
  public int x;
}

/* implicit conversion function: invalid return type */
public function {tto, tfrom} string foo___to(foo const& v) {
  return "abc";
}


{
  foo v;
  v.x = 100;
  int y = v;
  println(v);
  println(y);
}

