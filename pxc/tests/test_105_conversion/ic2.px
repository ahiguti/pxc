public namespace ic2;
import common -;
import operator -;

struct foo {
  int x;
}

/* implicit conversion function: invalid return type */
function {tto, tfrom} string foo___to(foo const& v) {
  return "abc";
}


{
  foo v;
  v.x = 100;
  int y = v;
  println(v);
  println(y);
}

