namespace ic3;
import common -;
import operator -;

struct foo {
  int x;
}

/* implicit conversion function: passby mutable reference is not allowed */
function {tto, tfrom} int foo___to(foo mutable& v) {
  return v.x;
}


{
  foo v;
  v.x = 100;
  int y = v; /* implicit conversion */
  println(v);
  println(y);
}

