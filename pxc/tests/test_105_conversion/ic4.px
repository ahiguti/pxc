public namespace ic4;
import common -;
import operator -;

public struct foo {
  public int x;
  /* implicit conversion function */
  public function {tto} int __to() const {
    return x;
  }
}

{
  foo v;
  v.x = 100;
  int y = v; /* implicit conversion */
  println(v);
  println(y);
}

