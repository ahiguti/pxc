namespace ut7;
public import core::common -;

function {f} int bar(int x)
{
  return f(x);
}

pure function int foo()
{
  int y = 3;
  int z = bar(y) int (int a) {
    return a + 1;
  }
  return z;
}

{
  int z = foo();
  println(z);
}
