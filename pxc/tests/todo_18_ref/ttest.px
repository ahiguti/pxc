
namespace ttest;
import thread "";
import io;

multithreaded struct test_thread_main : thread_main : {
  function void main() {
    io::println("thread main");
  }
}

thread_main_ref f = test_thread_main();
thread_ref thr = sys.create_thread(f);
thr.join();

