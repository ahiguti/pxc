public namespace conv2 "use-unsafe";
import common -;

struct foo {
  int x;
}

function extern "" {tto, tfrom} tto foo___to(foo const& x)
{
  return "hoge";
}

{
  foo v;
  slice{uchar} x = v; /* implicit conversion to noheap type is prohibited */
  println(x);
}
