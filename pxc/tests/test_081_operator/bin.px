public namespace bin;
import core::common -;
import core::operator -;

public struct foo {
  public int x;
  public double y;
}

public struct {t, n} modulo(t const& v0) {
  private t v = v0 % n;
}

{
  const v = foo(10, 5.5);
  const w = foo(25, 12.0);
  const z = v + w;
  println(v, w, z);
}

