namespace c5;
public import common -;
public import callable -;

struct foo {
  public function int __call(int x) { return x + x; }
}

struct bar {
  public foo z;
}

{
  bar b;
  const v = b.z(3);
  println(v);
}

