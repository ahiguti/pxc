public namespace ret;
import common -;
import meta m;

function {t} t foo(t x)
{
  if (m::eq{t, int}) {
    return x * x;
  } else {
  }
}

{
  int x = 100;
  const y = foo(x);
  println(y);
  long x1 = 100;
  const y1 = foo(x1);
  println(y1);
}
