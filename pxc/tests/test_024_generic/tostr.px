
public namespace tostr;
import common -;

threaded struct foo {
  public int x;
  public string y;
}

vector{foo} z;
z.push_back(foo(10, "abc"));
z.push_back(foo(20, "xyz"));
println(to_string(z));

