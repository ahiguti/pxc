public namespace nc1;
import core::common -;
import core::meta m;

metafunction val{x} m::add{x, x};

function void hoge(size_t v)
{
  println(v);
}

{
  size_t x = val{3};
  println(x);
  hoge(val{3});
}
