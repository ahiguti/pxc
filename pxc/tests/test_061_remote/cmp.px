public namespace cmp;
import common -;
import generic::ordered -;
import orb -;

private function void t1()
{
  println(compare_strict_weak{cstrref}("", "hoge"));
  println(compare_strict_weak{cstrref}("hoge", ""));
  println(compare_strict_weak{cstrref}("h", "hoge"));
  println(compare_strict_weak{cstrref}("hoge", "hoge"));
}

t1();

