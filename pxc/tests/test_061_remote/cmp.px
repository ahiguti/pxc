public namespace cmp;
import core::common -;
import core::generic::ordered -;
import orb -;

private function void t1()
{
  println(compare_weak{cstrref}("", "hoge"));
  println(compare_weak{cstrref}("hoge", ""));
  println(compare_weak{cstrref}("h", "hoge"));
  println(compare_weak{cstrref}("hoge", "hoge"));
}

t1();

