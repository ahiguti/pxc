public namespace uf;
public import common -;
public import io::errno -;

union foo {
  errno_t err;
  string value;
}

{
  exception::set_stack_trace_limit(0);
  foo x;
  x.err = EINTR;
  println(x.value); // throws
}

