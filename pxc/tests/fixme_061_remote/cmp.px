public namespace cmp;
import common -;
import ordered::compare -;
import orb -;

private function void t1()
{
  println(compare{cstrref}("", "hoge"));
  println(compare{cstrref}("hoge", ""));
  println(compare{cstrref}("h", "hoge"));
  println(compare{cstrref}("hoge", "hoge"));
}

t1();

