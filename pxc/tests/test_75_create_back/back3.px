namespace back3;
import common -;

struct foo {
  string s;
  int x;
  int y;
}

{
  exception::set_stack_trace_limit(0);
  vector{foo} v;
  v.construct_back("abc", 10, 20);
  foo const& f = v[0];
  v.construct_back("xyz", 15, 3); // throw
  println(v);
}

