public namespace th2;
public import common -;
public import thread::impl -;

threaded function int add(int x, int y)
{
  return x + y;
}

{
  mutable thd = thread::impl::thread{add}(10, 20);
  const r = thd();
  println(r);
}
