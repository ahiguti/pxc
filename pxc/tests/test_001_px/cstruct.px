namespace cstruct "use-unsafe";
import common -;

threaded struct extern "choge" hoge {
  int x;
  string y;
}

extern "functions" inline
struct choge {
  int x;
  pxcrt::bt_string y;
};
;

hoge v;
v.x = 100;
v.y = "abc";
println(v.x);
println(v.y);
