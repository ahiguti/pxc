public namespace tmpl;
import common -;

struct {T1, T2} my_pair {
  T1 first;
  T2 second;
}

function void test1()
{
  my_pair{int, int} pi;
  my_pair{double, double} pd;
  pi.first = 30;
  pi.second = 40;
  int x = pi.first + pi.second;
  println(x);
}

test1();

