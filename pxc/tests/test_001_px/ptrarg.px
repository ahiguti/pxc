import common -;

// vim: syntax=javascript:sw=2:ts=8

namespace ptrarg;

metafunction ulongptr ptr{ulong};
metafunction ulongcptr cptr{ulong};

function ulongptr addptr(ulongptr ap, ulongptr bp)
{
  return to_ptr{ulong}(*ap + *bp);
  ulongptr r = to_ptr{ulong}(*ap + *bp);
  return r;
}

struct foo {
  int fx;
  int fy;
}

{
  ulongptr x = to_ptr{ulong}(3UL);
  ulongptr y = to_ptr{ulong}(4UL);
  ulongcptr z = addptr(x, y);
  // var z:(cref ulong int) = addptr(x, y);
  // *z = 99;
  // &3 = z;
  println(*z);
}

{
  ptr{foo} fp = to_ptr{foo}(foo());
  fp->fx = 3;
  fp->fy = 4;
  println(fp->fx * fp->fy);
}

