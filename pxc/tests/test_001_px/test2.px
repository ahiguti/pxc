
namespace test2;
import common -;

metafunction ivector vector{int};

function int sum(ivector arr)
{
  size_t len = arr.size();
  int r = 0;
  size_t i;
  for (i = 0; i < len; ++i) {
    r += arr[i];
  }
  return r;
}

