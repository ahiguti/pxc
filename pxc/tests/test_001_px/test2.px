
public namespace test2;
import common -;

metafunction ivarray varray{int};

function int sum(ivarray arr)
{
  size_t len = arr.size();
  int r = 0;
  size_t i;
  for (i = 0; i < len; ++i) {
    r += arr[i];
  }
  return r;
}

