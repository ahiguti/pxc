import common -;

namespace constmet;

threaded struct foo {
  int x;
  function int m(int y) {
    return x + y;
  }
}

{
  ptr{foo} v = to_ptr(foo(10));
  println(v->m(20)); // calling non-const method. ok.
}
