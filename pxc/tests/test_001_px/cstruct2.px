public namespace cstruct2 "use-unsafe";
import core::common -;



pure struct extern "choge" hoge {
  public int x;
  public string y;
}

hoge v = hoge(200, "xyz"); # error
println(v.x);
println(v.y);
