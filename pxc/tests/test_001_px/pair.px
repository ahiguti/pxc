import common -;

public namespace pair;

struct {T} pair {
  T first;
  T second;
  function T rec(T x, T i)
  {
    if (i == 0) { return x; }
    return rec(x, i - 1) + rec(x, i - 1);
  }
  function T get()
  {
    return rec(first, 10) + rec(second, 10);
  }
}

