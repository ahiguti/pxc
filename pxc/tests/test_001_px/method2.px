import common -;

public namespace method2;

threaded struct foo {
  int v;
  function void add(int x) {
    v += x;
  }
  function void add2(int x, int y) {
    add(x);
    add(y);
  }
}

foo f = foo(3);
f.add2(15, 16);
println(f.v);

