public namespace ra "export-unsafe";
public import core::common -;
public import core::container::raw -;

{
  rawarray{int, 3} x;
  x[0u] = 3;
  x[1u] = 6;
  x[2u] = 9;
  const v = x[0u] + x[1u] + x[2u];
  println(v);
  println(x.to_cslice());
}

