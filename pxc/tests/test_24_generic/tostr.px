
namespace tostr;
import common -;
import text::conversion;

threaded struct foo {
  int x;
  string y;
}

vector{foo} z;
z.push_back(foo(10, "abc"));
z.push_back(foo(20, "xyz"));
println(text::conversion::to_string(z));

