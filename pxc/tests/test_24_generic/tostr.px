
namespace tostr;
import test_common "";
import generic;

struct foo {
  int x;
  string y;
}

vector{foo} z;
z.push_back(foo(10, "abc"));
z.push_back(foo(20, "xyz"));
io::println(generic::to_string(z));

