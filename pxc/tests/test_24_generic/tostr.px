
namespace tostr;
import test_common "";
import serialize::string;

struct foo {
  int x;
  string y;
}

vector{foo} z;
z.push_back(foo(10, "abc"));
z.push_back(foo(20, "xyz"));
println(serialize::string::to_string(z));

