public namespace nh5;
import common -;

{
  const x = make_vector{cstrref}("abc", "0123");
    /* ok. noheap container */
  const y = make_vector{cstrref}("xyz", "hoge");
  const z = make_vector{vector{cstrref}}(x, y);
  println(z);
}
