namespace thrtest3;
import common -;
import thread -;

struct my_thread_obj(int x0, int y0, io::io const& iop0) {
  int x = x0;
  int y = y0;
  io::io iop = iop0;
  function void main() {
    const stdout = iop.stdout();
    stdout.println("my thread main");
    stdout.println(x);
  }
}

thread thr = make_thread(my_thread_obj(123, 456, io::system));
  // error: my_thread_obj is not threaded
thr.join();

