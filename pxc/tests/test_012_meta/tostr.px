public namespace tostr;
import common -;

vector{int} a;
a.resize(5, int());
for (size_t k, int mutable v : a) {
  v = numeric_cast{int, size_t}(k);
}

threaded struct foo {
  public vector{int} a;
  public string b;
}

foo h;
h.a = a;
h.b = "hoge";

println(to_string(a));
println(to_string(h));

