public namespace trans;
import common -;
import meta m;

{
  metafunction x m::list{m::list{"a", "b", "c"}, m::list{"x", "y", "z"}};
  println(m::to_string{x});
  println(m::to_string{m::transpose{x}});
}
