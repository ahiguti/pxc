
namespace fe_root;
import test_common "";

ptr{vector{int}} p = ptr{vector{int}}(vector{int}());
io::println("a");
p->resize(10, int());
io::println("b");
foreach (size_t i, int& v : *p) {
  v = i;
  io::println(v);
}
foreach (size_t i, int& v : *p) {
  p = ptr{vector{int}}(vector{int}());
  v += i;
  io::println(v);
}
io::println("c");

