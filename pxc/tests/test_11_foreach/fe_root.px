
import io;

ref{vector{int}} p = ref(vector{int}());
io::println("a");
p->resize(10);
io::println("b");
foreach (size_t i, int& v : *p) {
  v = i;
  io::println(v);
}
foreach (size_t i, int& v : *p) {
  p = ref{vector{int}}();
  v += i;
  io::println(v);
}
io::println("c");

