
namespace fe_root;
import test_common "";

ptr{vector{int}} p = ptr{vector{int}}(vector{int}());
println("a");
p->resize(10, int());
println("b");
foreach (size_t i, int mutable v : *p) {
  v = i;
  println(v);
}
foreach (size_t i, int mutable v : *p) {
  p = ptr{vector{int}}(vector{int}());
  v += i;
  println(v);
}
println("c");

