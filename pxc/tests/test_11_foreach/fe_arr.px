
namespace fe_arr;
import test_common "";

vector{string} a;
a.resize(100, "");
foreach (size_t i, string mutable s : a) {
  s = i.to_string();
}
foreach (size_t i, string s : a) {
  println(a[i]);
}

