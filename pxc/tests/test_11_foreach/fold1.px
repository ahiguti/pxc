namespace fold1;
import common -;

import meta::builtin m;

function int foo(int v, int x, int y, int z, int w)
{
  return v + x + y + z + w;
}

function int foostr(string x, string y, string z)
{
  int r;
  foreach (i : m::list{"x", "y", "z"};
    j : type::builtin::string_to_bt_int(i); ",") {
    r = foo(2, j, 6);
  }
  return r;
}

println(foostr("3", "4", "5"));

