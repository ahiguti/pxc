
import io;
import container;

function int test1(container::vector{int}& a)
{
  foreach (size_t k, int& v : a) {
    v += v;
  }
  return 0;
}

container::vector{int} a.resize(10);
a[5] = 30;
test1(a);
io::println(a[5]);
