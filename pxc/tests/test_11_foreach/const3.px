namespace const3;
import test_common "";

import io;

function int test1(vector{int}& a)
{
  foreach (size_t k, int& v : a) {
    v += v;
  }
  return 0;
}

vector{int} a;
a.resize(10, int());
a[5] = 30;
test1(a);
io::println(a[5]);
