
namespace fe_sli2;
import common -;

function void t1() {
  vector{string} a;
  a.resize(100, "");
  slice{string} z = a;
  for (size_t i, string mutable s : z) {
    s = to_string(i);
  }
  for (size_t i, string s : z) {
    println(a[i]);
  }
  a.resize(5, ""); // would invalidate
}
t1();
