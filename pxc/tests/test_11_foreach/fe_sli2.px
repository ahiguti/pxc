
namespace fe_arr;
import test_common "";

vector{string} a;
a.resize(100, "");
slice{string} z = a;
foreach (size_t i, string& s : z) {
  s = i.to_string();
}
foreach (size_t i, string s : z) {
  println(a[i]);
}
a.resize(5, ""); // would invalidate

