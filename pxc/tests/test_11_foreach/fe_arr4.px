
namespace fe_arr4;
import test_common "";

function void test1(vector{string}& a)
{
  foreach (size_t i, string mutable s : a) {
    s = i.to_string();
  }
  foreach (size_t i, string s : a) {
    println(a[i]);
  }
}

vector{string} a;
a.resize(100, string());
test1(a);
