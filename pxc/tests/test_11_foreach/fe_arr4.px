
namespace fe_arr4;
import test_common "";

function void test1(vector{string}& a)
{
  foreach (size_t i, string& s : a) {
    s = size_t_to_string(i);
  }
  foreach (size_t i, string s : a) {
    io::println(a[i]);
  }
}

vector{string} a;
a.resize(100);
test1(a);
