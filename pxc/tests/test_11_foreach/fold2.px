
import meta;
import io;

function int foo(int v, int x, int y, int z, int w)
{
  return v + x + y + z + w;
}

function int foostr(string x, string y, string z)
{
  int r;
  foreach (i : meta::argnames{foostr}; j : string_to_int(i); ",") {
    r = foo(2, j, 6);
  }
  return r;
}

io::println(foostr("3", "4", "5"));

