private namespace tmpl;
public import core::common -;

public pure tsvaluetype struct
keytype(int v0)
{
  public int v = v0; /* is_accessible_namespace */
}

{
  tree_set{keytype} m;
  m[keytype(5)];
  m[keytype(6)];
  println(m.size());
}
