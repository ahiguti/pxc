
namespace closure6;
import io;

function void test()
{
  int x = 0;
  function {t} void rec(t v) {
    if (v <= 0) {
      return;
    }
    x += v;
    rec{t}(v - 1);
  }
  rec{int}(5);
  io::println(x);
}

test();

