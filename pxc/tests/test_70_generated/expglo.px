namespace expglo;
import common -;
import meta m;

/* error: expand is not allowed in the global block */
expand(sym, idx: m::list{"foo", "bar", "baz"}) {
  function string sym() {
    return m::to_string{sym};
  }
}

{
  println("hoge");
}

