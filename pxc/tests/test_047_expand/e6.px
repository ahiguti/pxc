public namespace e6;
import common -;
import meta m;

interface ifoo {
  function string hoge(string x);
  function string fuga(string x);
}

interface ibar {
  function string abc(string x);
  function string xyz(string x);
}

metafunction mf_str{t} m::map{m::member_functions{t}, meta::to_string};

struct {ityp} foo < ityp > {
  expand(fsym : mf_str{ityp}) {
    function string fsym(expand(m::args{m::symbol{ityp, fsym}})) {
      string s = m::to_string{fsym};
      s.append(":");
      s.append(x);
      s.append(":");
      metafunction mf m::symbol{ityp, fsym};
      s.append(m::full_string{m::arg_types{mf}});
      return s;
    }
  }
}
/*
*/

function void test1() {
  println(m::full_string{m::member_functions{ifoo}});
  const p = make_ptr{foo{ifoo}}(foo{ifoo}());
  println(p->hoge("A"));
  println(p->fuga("B"));
}

test1();
