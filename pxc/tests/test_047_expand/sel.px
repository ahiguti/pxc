namespace sel;
import common -;
import meta m;

{
  expand (sym : m::list{"a", "b", "c"}) {
    println(m::to_string{sym});
  }
  expand (sym : m::list{"a", "b", "c"}) {
    println(m::to_string{sym}, 0);
    println(m::to_string{sym}, 1);
    println(m::to_string{sym}, 2);
  }
}
