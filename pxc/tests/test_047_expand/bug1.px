public namespace bug1;
public import common -;
public import meta m;

public metafunction flag 1;

public function void foo()
{
  expand(s: m::seq{m::ne{flag, 0}}) {
    println("on");
  }
  expand(s: m::seq{m::eq{flag, 0}}) {
    println("off");
  }
}

foo();
