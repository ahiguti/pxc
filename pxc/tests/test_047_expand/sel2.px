public namespace sel2;
import common -;
import meta m;

{
  expand (sym : m::list{m::list{"a", 0}, m::list{"b", m::list{2, 1, 0}}}) {
    println(m::to_string{sym}, 0);
    println(m::to_string{sym}, 1);
    println(m::to_string{sym}, 2);
  }
}
