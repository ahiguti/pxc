
public namespace const10;
import common -;

function void test1()
{
  vector{int} f;
  f.resize(10, int());
  f[3] = 99;
  slice{int} s = f[0..5];
  cslice{int} cs = s; // ok to convert from slice to cslice
  println(cs[3]);
}

test1();
