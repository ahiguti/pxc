
public namespace autoconv4;
import common -;

function void test1()
{
  vector{int} x;
  x.resize(10, int());
  x[3] = 99;
  slice{int} s = x; // vector to slice
  s[3] = 98;
  x.resize(12, int()); // would invalidate
  println(x[3]);
}

exception::set_stack_trace_limit(0);
test1();
