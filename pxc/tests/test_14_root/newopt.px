namespace newopt;
import test_common "";

import io;

struct foo {
  int x;
  int y;
}

function void test()
{
  ptr{foo} p = ptr{foo}(foo());
  p->x = 3;
  p->y = 4;
  int z = p->y;
  io::println(z);
}

test();
