namespace byref1;
import test_common "";

function int foo(int& x)
{
  x += 2;
  return x;
}

function int bar(int x)
{
  return x * 2;
}

int r = foo(bar(3)); // can not modify
import io;
io::println(r);

