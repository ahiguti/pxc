namespace ptr;
import test_common "";

import io;

function void test2(ptr{int} pr, int& ir)
{
  // pr = ptr{int}();
  ir = 3;
}

function void test()
{
  ptr{int} p = ptr{int}(int());
  test2(p, *p);
  io::println(*p);
}

test();
