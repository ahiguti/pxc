
namespace arr;
import common -;

function int foo(int& x)
{
  x += 2;
  return x;
}

ptr{vector{int}} ap = ptr{vector{int}}(vector{int}());
ap->resize(10, int());
(*ap)[1] = 99;
int x = foo((*ap)[1]);
int y = (*ap)[1];

println(x);
println(y);

