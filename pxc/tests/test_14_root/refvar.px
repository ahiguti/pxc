
function int foo(int& x)
{
  x += 2;
  return x;
}

function int bar()
{
  vector{vector{int}} aa;
  aa.resize(10);
  aa[3].resize(10);
  aa[3][1] = 99;
  int mutable & x = aa[3][1];
  int y = foo(aa[3][1]);
  return x + y;
}

import io;
io::println(bar()); // FIXME: should be 202

