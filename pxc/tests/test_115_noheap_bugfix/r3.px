namespace r3;
import common -;

{
  exception::set_stack_trace_limit(0);
  mutable vec1 = make_vector{int}(1, 2, 3, 4, 5, 6);
  mutable vec2 = make_vector{int}(1, 2, 3, 4, 5, 6);
  const vv = make_vector{cslice{int}}(vec1, vec2);
    // must root vector elements also
  println(vv);
  vec2.resize(1000, 0); // must throw would_invalidate
  println(vv);
}
