namespace r2;
import common -;

{
  exception::set_stack_trace_limit(0);
  mutable vec = make_vector{int}(1, 2, 3, 4, 5, 6);
  const vv = make_vector{cslice{int}}(vec); // must root elements of vec also
  println(vv);
  vec.resize(1000, 0); // must throw would_invalidate
  println(vv);
}
