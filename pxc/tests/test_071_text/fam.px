public namespace fam;
import core::common -;
import core::meta m;
import core::meta::family mf;

{
  println(mf::is_string_or_slice_type{string});
  println(mf::is_string_or_slice_type{strref});
  println(mf::is_string_or_slice_type{cstrref});
  println(mf::is_string_or_slice_type{farray{uchar, 5}});
  println(mf::is_string_or_slice_type{darray{uchar}});
  println(mf::is_string_or_slice_type{strlit});
}
