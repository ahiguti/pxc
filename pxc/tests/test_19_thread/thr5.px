
namespace ttest;
import thread "";
import io;

multithreaded struct my_thread_obj {
  int x;
  int y;
  io::ioref io;
  function void main() {
    io.println("my thread main");
    io.println(x);
    io.println(y);
  }
}

function void test1()
{
  io::ioref i = io::io(); // error: io is not default-constructible
  thread_t thr = create_thread(my_thread_obj(123, 456, io::main));
  thr.join();
}

test1();

