namespace tref;
import test_common "";


multithreaded struct foo {
  int x;
  int y;
  function int get_x() const { return x; }
  function int get_y() const { return y; }
  function int get_sum() const { return x + y; }
}

tptr{foo} p1 = tptr{foo}(foo(5, 10));
io::standard::stdout_println(io::io, p1->get_sum());

