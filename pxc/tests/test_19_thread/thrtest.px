import test_common "";

namespace ttest;
import thread "";
import io;

multithreaded struct my_thread_obj(int x0, int y0, io::ioptr iop) {
  int x = x0;
  int y = y0;
  io::ioptr io = iop;
  function void main() {
    io.stdout_println("my thread main");
    io.stdout_println(x);
    io.stdout_println(y);
  }
}

function void test1()
{
  thread_t thr = create_thread(my_thread_obj(123, 456, io::main));
  thr.join();
}

test1();

