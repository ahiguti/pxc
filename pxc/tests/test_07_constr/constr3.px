
namespace constr3;
import io;

struct foo(int i) {
  int iv = m1(i);
  string sv = "hoge";
  function int m1(int i) {
    return iv + i + 100;
  }
}

ref{foo} f = ref(foo(10));
io::println(f->iv);
io::println(f->sv);
