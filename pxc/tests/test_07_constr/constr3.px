import test_common "";

namespace constr3;
import io;

struct foo(int i) {
  int iv = m1(i); // calling member function from a constructor
  string sv = "hoge";
  function int m1(int i) {
    return iv + i + 100;
  }
}

ptr{foo} f = ptr{foo}(foo(10));
io::println(f->iv);
io::println(f->sv);
