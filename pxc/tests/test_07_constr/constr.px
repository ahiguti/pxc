import test_common "";

namespace constr;
import io;

struct bar {
}

struct foo(int i, string s) {
  int iv = i + 10;
  string sv = "hoge";
}

ptr{foo} f = ptr{foo}(foo(0, ""));
io::println(f->iv);
io::println(f->sv);
