import test_common "";

namespace constr;

struct bar {
}

struct foo(int i, string s) {
  int iv = i + 10;
  string sv = "hoge";
}

ptr{foo} f = ptr{foo}(foo(0, ""));
println(f->iv);
println(f->sv);
