import test_common "";

namespace constr3;
import io;

struct foo(int i) {
  int iv = i;
  string sv = "hoge";
  function int m1(int i) {
    return iv + i + 100;
  }
}

struct bar {
  int x;
  foo f = foo(0);
}

union baz {
  int x;
  foo f;
}

vector{bar} b; # ok
baz bz; # ok
io::println("ok");

