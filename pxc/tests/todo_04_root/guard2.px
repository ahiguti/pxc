
namespace guard1;
import io;

function int test1() {
  function void foo(vector{int}& vec, int v) {
    vec.resize(100); // ok because v is passed by value
    vec[90] = 30;
  }
  vector{int} v;
  v.resize(10);
  foo(v, v[1]);
  return v[90];
}

int v = test1();
println(v);

