public namespace tbl3;
import common -;
import io::text -;

mtvaluetype struct ent {
  string key;
  int x;
  int y;
}

{
  vector{ent} es;
  const err = system.read_append_records{0}("hoge.txt", es);
  println(es);
}

