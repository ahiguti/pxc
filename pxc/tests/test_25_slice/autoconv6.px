
namespace autoconv1;
import common "";

function void test1()
{
  vector{int} x;
  x.resize(10, int());
  x[3] = 99;
  cslice{int} s = x; // vector to cslice
  x.resize(12, int()); // would invalidate
  println(x[3]);
}

test1();
