
namespace slife;
import test_common "";

function int foo(slice{int} s, int i)
{
  foreach (size_t j, int e : s) {
    io::println(e);
  }
  return i;
}

function void t1()
{
  vector{int} v;
  v.resize(100);
  for (size_t i = 10; i < 20; ++i) {
    v[i] = i * 2;
  }
  int r = foo(v[10..20], 5);
  // io::println(r);
}

t1();

