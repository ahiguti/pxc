
namespace const5;
import test_common "";

function void test1()
{
  vector{int} a;
  a.resize(100);
  for (int i = 0; i < 100; ++i) { a[i] = i * 2; }
  cslice{int} asli = a[30..100]; // ok
  io::println(asli[0]);
  asli[50] = 3; // error
}

test1();
