
namespace grd;
import test_common "";

function void test1() {
  vector{int} f;
  f.resize(100);
  for (int i = 0; i < 100; ++i) { f[i] = i * 2; }
  {
    int const & e1 = f[15];
    // slice{int} s1 = f[0..50];
    f.resize(0); // throws
  }
}

test1();

