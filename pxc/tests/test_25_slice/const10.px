
namespace const8;
import test_common "";

function void test1()
{
  vector{int} f;
  f.resize(10);
  f[3] = 99;
  slice{int} s = f[0..5];
  cslice{int} cs = s; // ok to convert from slice to cslice
  io::println(cs[3]);
}

test1();
