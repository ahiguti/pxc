
namespace autoconv1;
import test_common "";

function int f1(cslice{int} const va, uint i)
{
  return va[i];
}

function void test1()
{
  vector{int} x;
  x.resize(10);
  x[3] = 99;
  int r = f1(x, 3); // vector to cslice
  println(r);
}

test1();
