
namespace const2;
import common "";

function void test1()
{
  vector{int} a;
  a.resize(100, int());
  int i;
  for (i = 0; i < 100; ++i) { a[i] = i * 2; }
  cslice{int} asli = a[30..100]; // ok
  println(asli[0]);
  vector{int} b = a;
  cslice{int} bsli = b[50..100]; // ok
  println(bsli[1]);
}

test1();
