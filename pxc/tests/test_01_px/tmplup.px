
namespace tmplup;
import meta;
import io;

function void test1() {
  int v = 0;
  int w = 0;
  function {f} meta::argtype{f, 0} apply() {
    return f(v * v);
  }
  function int foo(int x) {
    return x + w;
  }
  v = 10;
  w = 20;
  int z = apply{foo}();
  io::println(z);
}

test1();

