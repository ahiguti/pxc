import test_common "";

namespace constmet5;

struct foo {
	int x;
	function int n(int y) {
		int oldx = x;
		x = y;
		return oldx;
	}
}

cptr{foo} v = cptr{foo}(foo(10));
println(v->n(20)); // error

