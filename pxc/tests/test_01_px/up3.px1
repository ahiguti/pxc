
namespace up3;
import test_common "";

function {t, f} void vector_values(vector{t}& v)
{
  uint sz = v.size();
  for (uint i = 0; i < sz; ++i) {
    f(v[i]);
  }
}

function {t, f} void vector_keys(vector{t}& v)
{
  uint sz = v.size();
  for (uint i = 0; i < sz; ++i) {
    f(i);
  }
}

function void foo(vector{int}& v) {
  int x;
  v.values{int, function void (int e) { x += e; }}();
  io::println(x);
}

vector{int} a.resize(101);
for (int i = 0; i < a.size(); ++i) {
  a[i] = i;
}
foo(a);

