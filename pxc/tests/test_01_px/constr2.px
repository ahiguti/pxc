
namespace constr;
import io;

struct complex {
  int re;
  int im;
}

function int foo(int x)
{
  complex z = complex(string_to_int("123"), x);
  complex w = complex(string_to_int("123"), string_to_int("456"));
  return z.re + z.im;
}

io::println(foo(3));
