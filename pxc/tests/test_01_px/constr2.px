import test_common "";

namespace constr;
import io;

struct complex {
  int re;
  int im;
}

function int foo(int x)
{
  complex z = complex(type::builtin::string_to_bt_int("123"), x);
  complex w = complex(type::builtin::string_to_bt_int("123"),
    type::builtin::string_to_bt_int("456"));
  return z.re + z.im;
}

io::println(foo(3));
