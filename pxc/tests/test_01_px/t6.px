import test_common "";

import io;

namespace tmpl;

struct {T} foo {
  T value;
}

function void test1()
{
  foo{int} vi;
  foo{long} vd;
  // pi.first = 30;
  // pi.second = 40;
  // int x = pi.first + pi.second;
  io::println(vd.value);
}

test1();

