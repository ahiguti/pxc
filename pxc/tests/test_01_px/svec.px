namespace svec;
import io;

extern "pxcrt::vector" "varray" struct {t} svec {
  extern "size" function ulong size();
  extern "resize" function void resize(ulong sz);
}

svec{int} sv;
sv.resize(100);
sv[10] = 3;
io::println(sv[10]);

