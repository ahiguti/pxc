import common -;

namespace memfld;

threaded struct foo {
  int x;
  int y;
  function void init(int v, int w) {
    x = v;
    y = w;
  }
  function int sum() {
    return x + y;
  }
}

foo f;
f.init(10, 30);
println(f.sum());
