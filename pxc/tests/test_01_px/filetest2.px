import test_common "";
namespace filetest;

function void ft1()
{
  string fn = "hoge.txt";
  io::file::statbuf stbuf;
  int r = io::file::system_stat(io::system::system, fn, stbuf);
  if (r != 0) {
    print("failed to stat: ");
    println(fn);
  } else {
    println(stbuf.st_size);
  }
}

function void ft2()
{
  string s;
  io::file::system_read_file(io::system::system, "hoge.txt", s);
  print(s);
}

ft1();
ft2();

