import test_common "";
namespace filetest;

function void ft1()
{
  string fn = "hoge.txt";
  io::file::statbuf stbuf;
  io::errno::errno_t r = io::file::io_stat(io::io, fn, stbuf);
  if (r != 0) {
    print("failed to stat: ");
    println(fn);
  } else {
    println(stbuf.st_size);
  }
}

function void ft2()
{
  string s;
  io::file::io_read_file(io::io, "hoge.txt", s);
  print(s);
}

ft1();
ft2();

