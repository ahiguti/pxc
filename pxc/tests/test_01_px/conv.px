
namespace test;
private import io;

function uint fib(uint x)
{
  if (x <= 0) {
    return 0;
  } else if (x <= 1) {
    return 1;
  } else {
    return fib(x - 1) + fib(x - 2);
  }
}

string s = uint_to_string(fib(30));
int si = string_to_int(s);
io::println(si);
io::println(fib(10));

