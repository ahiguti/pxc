import test_common "";

namespace constmet;
import io;

struct foo {
	int x;
	function int m(int y) {
		return x + y;
	}
}

ptr{foo} v = ptr{foo}(foo(10));
io::println(v->m(20)); // calling non-const method. ok.

