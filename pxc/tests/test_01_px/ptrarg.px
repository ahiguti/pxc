import test_common "";

// vim: syntax=javascript:sw=2:ts=8

namespace ptrarg;
private import io;

macro ulongptr ptr{ulong};
macro ulongcptr cptr{ulong};

function ulongptr addptr(ulongptr ap, ulongptr bp)
{
  return ulongptr(*ap + *bp);
  ulongptr r = ulongptr(*ap + *bp);
  return r;
}

struct foo {
  int fx;
  int fy;
}

{
  ulongptr x = ulongptr(3UL);
  ulongptr y = ulongptr(4UL);
  ulongcptr z = addptr(x, y);
  // var z:(cref ulong int) = addptr(x, y);
  // *z = 99;
  // &3 = z;
  io::println(*z);
}

{
  ptr{foo} fp = ptr{foo}(foo());
  fp->fx = 3;
  fp->fy = 4;
  io::println(fp->fx * fp->fy);
}

