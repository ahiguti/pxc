
namespace cm1;
import io;

struct foo {
  function int m1() {
    return 3;
  }
  function int m2(foo& f) const {
    return f.m1();
      // ok to call a non-const memfunc because it has an explicit obj
  }
}

foo x;
int r = x.m2(x);
io::println(r);

