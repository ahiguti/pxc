
import io;

struct bar {
  int v;
  int w;
}

function int fubar(ref{bar} x)
{
  return x->v + x->w;
}

function int foo(cref{bar} x)
{
  return x->v + x->w;
}

{
  bar z;
  z.v = 3;
  z.w = 4;
  io::println(foo(z));
}

