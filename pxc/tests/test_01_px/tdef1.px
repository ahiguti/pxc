namespace tdef1;
import common -;


metafunction ptrint ptr{int};

function ptrint ptradd(ptrint x, ptrint y)
{
  return box{int}(*x + *y);
}

{
  ptrint x = box{int}(int());
  *x = 5;
  ptrint y = box{int}(int());
  *y = 8;
  println(*ptradd(x, y));
}

