namespace tdef1;
import common -;


metafunction ptrint ptr{int};

function ptrint ptradd(ptrint x, ptrint y)
{
  return to_ptr{int}(*x + *y);
}

{
  ptrint x = to_ptr{int}(int());
  *x = 5;
  ptrint y = to_ptr{int}(int());
  *y = 8;
  println(*ptradd(x, y));
}

