namespace tdef1;
import common -;


metafunction ptrint ptr{int};

function ptrint ptradd(ptrint x, ptrint y)
{
  return ptrint(*x + *y);
}

{
  ptrint x = ptrint(int());
  *x = 5;
  ptrint y = ptrint(int());
  *y = 8;
  println(*ptradd(x, y));
}

