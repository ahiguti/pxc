
namespace klass;
private import io;

interface ifoo {
  function int m1(int x, int y);
  function string m2(string x);
}

struct foo {
  int z;
  function int m1(int x, int y) {
    return x + y + z;
  }
}

foo f;
f.z = 3;
io::println(f.z);
int w = f.m1(5, 6);
io::println(w);

