
namespace up1;
import io;

function int foo(int x)
{
  int z = 3;
  function int bar(int y)
  {
    x += 3; # can not be modified!
    return y + x + z;
  }
  return x * bar(3);
}

io::println(foo(5));

