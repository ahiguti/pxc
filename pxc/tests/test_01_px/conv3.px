import common -;
namespace conv3;
import myarray;

function void t1()
{
  vector{int} arr;
  string s = "abc";
  cstrref s0 = s;
  myarray::push(arr, string::positional::decimal_to_integral{int}(s0));
    /* fails to parse */
  println(s0.size()); /* remains 3 */
}

t1();
