
import io;

function int f1(ref{int}& x, cref{int}& y)
{
  *x += 1;
  *y += 1; // can not modify ...
  return *x + *y;
}

function void test1()
{
  ref{int} x = int(0);
  *x = 3;
  ref{int} y = int(0);
  *y = 4;
  io::println(f1(x, y));
  io::println(*x);
}

test1();

