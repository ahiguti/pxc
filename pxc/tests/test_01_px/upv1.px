
namespace upv1;
import io;

int w = 3;
struct foo {
  int f;
}
function void test(foo o) {
  int x;
  function void bar(int y) {
    x += y;
    x += o.f;
    x += w;
  }
  bar(3);
  io::println(x);
}

foo o;
o.f = 3;
test(o);
