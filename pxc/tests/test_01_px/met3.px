import test_common "";

namespace met2;
import testrec;
import io;

function void t1()
{
  ptr{testrec::foo} f = ptr{testrec::foo}(testrec::foo());
  f->x = 100;
  int v = f->m1(2, 3);
  io::println(v); 
  int v2 = f->m2(2, 3);
  io::println(v2); 
}

t1();
