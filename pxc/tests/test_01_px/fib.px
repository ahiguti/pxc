
namespace fib;
import io;

function {t} t fib(t x)
{
  if (x <= 1) { return x; }
  macro tfib fib{t};
  return tfib(x - 2) + tfib(x - 1);
}

macro intfib fib{int};

int x = intfib(10);
io::println(x);

