import common -;
namespace filetest;

function void ft1()
{
  io::errno::errno_t e;
  var f = io::file::io_open(io::system, "hoge.txt", io::file::O_RDONLY, 0, e);
  string s;
  size_t len = 3;
  f.read(s, len, e);
  println(s);
}

function void ft2()
{
  string s;
  io::errno::errno_t e;
  io::file::io_read_file(io::system, "hoge.txt", s, e);
  print(s);
}

ft1();
ft2();

