import test_common "";
namespace filetest;

function void ft1()
{
  io::errno::errno_t e;
  var f = io::file::file(io::io, "hoge.txt", io::file::O_RDONLY, 0, e);
  string s;
  s.resize(3, 0);
  size_t len;
  f.read(s, len);
  println(s);
}

function void ft2()
{
  string s;
  io::file::io_read_file(io::io, "hoge.txt", s);
  print(s);
}

ft1();
ft2();

