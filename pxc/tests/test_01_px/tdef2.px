
import io;

macro ptrint ptr(int);

function ptradd(x:ptrint, y:ptrint):ptrint
{
  return *x + *y;
}

var x:ptrint = ptrint();
*x = 5; // throws
var y:ptrint = ptrint();
*y = 8;
io::println(*ptradd(x, y));

