
import io;

macro refint ref(int);

function refadd(x:refint, y:refint):refint
{
  return *x + *y;
}

var x:refint = refint();
*x = 5; // throws
var y:refint = refint();
*y = 8;
io::println(*refadd(x, y));

