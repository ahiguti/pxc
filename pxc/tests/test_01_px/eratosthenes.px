
import array;
import io;

uint x = 100000;
vector{int} arr;
# arr[x] = 0;
array::resize(arr, x);
arr[1] = 1;
for (uint i = 2; i < x; ++i) {
  if (arr[i] == 0) {
    io::println(i);
    for (uint j = i * i; j < x; j += i) {
      arr[j] = 1;
    }
  }
}

