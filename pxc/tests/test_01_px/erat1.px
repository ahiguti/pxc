
namespace erat1;
import test_common "";

function ref{vector{uint}} eratosthenes(int x)
{
  vector{uint} uiv;
  ref{vector{uint}} ret = ref(uiv);
  vector{uint} sieve;
  sieve.resize(x);
  sieve[1] = 1;
  for (uint i = 2; i < x; ++i) {
    if (sieve[i] == 0) {
      ret->push(i);
      for (uint j = i * i; j < x; j += i) {
	sieve[j] = 1;
      }
    }
  }
  return ret;
}

ref{vector{uint}} arr = eratosthenes(100000);
for (uint i = 0; i < arr->size(); ++i) {
  io::println((*arr)[i]);
}

