import common -;

// vim: syntax=javascript:sw=2:ts=8

namespace ptrtest;

metafunction ulongptr ptr{ulong};
metafunction ulongcptr cptr{ulong};

function ulongptr addptr(ulongptr ap, ulongptr bp)
{
  return box{ulong}(*ap + *bp);
  ulongptr r = box{ulong}(*ap + *bp);
  return r;
}

struct foo {
  int fx;
  int fy;
}

{
  ulongptr x = box{ulong}(3UL);
  ulongptr y = box{ulong}(4UL);
  ulongcptr z = addptr(x, y);
  // var z:(cptr ulong int) = addptr(x, y);
  // *z = 99;
  // &3 = z;
  println(*z);
}

{
  ptr{foo} fp = box{foo}(foo());
  fp->fx = 3;
  fp->fy = 4;
  println(fp->fx * fp->fy);
}

