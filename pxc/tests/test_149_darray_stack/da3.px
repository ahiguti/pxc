namespace da3;
public import common -;

public metafunction dstint darrayst{int};

public function dstint damul(slice{int} const& s)
{
  dstint arr = s;
  return arr;
}

{
  mutable a = make_varray{int}(1, 2, 3, 4, 5);
  const x = damul(a);
  println(x);
}

