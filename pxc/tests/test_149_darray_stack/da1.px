namespace da1;
public import core::common -;

public function int damul(size_t sz, int val)
{
  const arr = darrayst{int}(sz, val);
  int r = 1;
  for (size_t i, int v : arr) {
    r *= v;
  }
  return r;
}

{
  const x = damul(5, 10);
  println(x);
}

