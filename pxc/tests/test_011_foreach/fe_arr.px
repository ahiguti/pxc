
public namespace fe_arr;
import core::common -;

varray{string} a;
a.resize(100, string());
for (size_t i, string mutable& s : a) {
  s = to_string(i);
}
for (size_t i, string const& s : a) {
  println(a[i]);
}

