
public namespace fe_arr4;
import common -;

function void test1(vector{string} mutable& a)
{
  for (size_t i, string mutable s : a) {
    s = to_string(i);
  }
  for (size_t i, string s : a) {
    println(a[i]);
  }
}

vector{string} a;
a.resize(100, string());
test1(a);
