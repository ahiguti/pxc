namespace va;
import common "";


// see fa.px

function void t1() 
{
  vector a;
  a.resize(100, 0);
  int i;
  for (i = 0; i < a.size(); ++i) {
    a[i] = i;
  }
  for (i = 0; i < a.size(); ++i) {
    println(a[i]);
  }
}

t1();
