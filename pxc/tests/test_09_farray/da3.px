namespace da3;
import common -;


// see fa.px

function void t1() 
{
  mutable a = darray{int}(100U, 0);
  a = darray{int}(100U, 0);
  int i;
  for (i = 0; i < a.size(); ++i) {
    a[i] = i;
  }
  for (i = 0; i < a.size(); ++i) {
    println(a[i]);
  }
}

t1();
