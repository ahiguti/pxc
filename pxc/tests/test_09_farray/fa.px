namespace fa;
import test_common "";


function void t1() 
{
  farray{int, 100} a;
  int i;
  for (i = 0; i < a.size(); ++i) {
    a[i] = i;
  }
  for (i = 0; i < a.size(); ++i) {
    println(a[i]);
  }
}

t1();
