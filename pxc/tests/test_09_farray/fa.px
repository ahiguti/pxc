namespace fa;
import test_common "";

import io;

function void t1() 
{
  farray{int, 100} a;
  int i;
  for (i = 0; i < a.size(); ++i) {
    a[i] = i;
  }
  for (i = 0; i < a.size(); ++i) {
    io::println(a[i]);
  }
}

t1();
