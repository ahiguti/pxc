namespace fa2;
import test_common "";


function void t1() 
{
  farray{int, int} a; # error
  int i;
  for (i = 0; i < a.size(); ++i) {
    a[i] = i;
  }
  for (i = 0; i < a.size(); ++i) {
    println(a[i]);
  }
}

t1();
