namespace fa2;
import test_common "";

import io;

function void t1() 
{
  farray{int, int} a; # error
  int i;
  for (i = 0; i < a.size(); ++i) {
    a[i] = i;
  }
  for (i = 0; i < a.size(); ++i) {
    io::println(a[i]);
  }
}

t1();
