namespace da2;
import common -;


function void t1() 
{
  mutable a = darray(100U, 0);
  size_t i;
  for (i = 0; i < a.size(); ++i) {
    a[i] = static_cast{int}(i);
  }
  for (i = 0; i < a.size(); ++i) {
    println(a[i]);
  }
}

t1();
