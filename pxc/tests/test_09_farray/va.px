namespace va;
import test_common "";

import io;

// see fa.px

function void t1() 
{
  vector{int} a;
  a.resize(100, 0);
  int i;
  for (i = 0; i < a.size(); ++i) {
    a[i] = i;
  }
  for (i = 0; i < a.size(); ++i) {
    io::println(a[i]);
  }
}

t1();
