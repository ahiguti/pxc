namespace fptr;
import common -;
import io::file -;

function void t1()
{
  var eh = io::errno::errno_handler(error::handle_keep);
  var f = io_open(io::system, "hoge.txt", io::file::O_RDONLY, 0, eh);
  string s;
  f.read_all(s, eh);
  print(s);
}

t1();

