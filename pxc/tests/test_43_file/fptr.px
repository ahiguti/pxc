namespace fptr;
import common -;
import io::file -;

function void t1()
{
  io::errno::errno_t ern;
  var f = io_open(io::system, "hoge.txt", io::file::O_RDONLY, 0, ern);
  string s;
  f.read_all(s, ern);
  print(s);
}

t1();

