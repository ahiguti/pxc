namespace missingret;
import test_common "";

import generic;

function {x} int foo()
{
  if (meta::eq{x, 0}) {
    return 0;
  } else if (meta::eq{x, 1}) {
    // return 1;
  } else {
    return 2;
  }
}

int x = foo{1}();

import io;

io::println(x);

