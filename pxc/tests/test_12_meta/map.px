namespace map;
import test_common "";

import meta::builtin m;

macro sq{x} m::mul{x, x};
macro lst m::list{1, 2, 3, 4};
macro rlst m::metamap{lst, sq};
println(m::to_string{lst});
println(m::to_string{rlst});

