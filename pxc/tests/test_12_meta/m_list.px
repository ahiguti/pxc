namespace m_list;
import test_common "";

import meta::builtin m;

macro foo m::list{"abc", 3, 99, "hoge"};

println(m::at{foo, 0});
println(m::at{foo, 1});
println(m::at{foo, 2});
println(m::at{foo, 3});

