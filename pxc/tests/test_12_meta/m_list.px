namespace m_list;
import test_common "";

import meta;
import io;

macro foo meta::list{"abc", 3, 99, "hoge"};

io::println(meta::at{foo, 0});
io::println(meta::at{foo, 1});
io::println(meta::at{foo, 2});
io::println(meta::at{foo, 3});

