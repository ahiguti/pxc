namespace test_symbols2;
import common -;
import symbols;

struct {t} hoge {
  int x;
  t y;
}

struct foo {
  function {t} t bar(t x) { return t; }
}

public function {ns} void show_exports()
{
  foreach (symbol : symbols::all_functions{ns}) {
    println(meta::builtin::full_string{symbol});
  }
  foreach (symbol : symbols::all_types{ns}) {
    println(meta::builtin::full_string{symbol});
  }
}

show_exports{"test_symbols2"}();

