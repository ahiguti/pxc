namespace test_symbols;
import test_common "";
import symbols;

public function {ns} void show_exports()
{
  foreach (symbol : symbols::all_functions{ns}) {
    io::println(meta::full_string{symbol});
  }
  foreach (symbol : symbols::all_types{ns}) {
    io::println(meta::full_string{symbol});
  }
}

show_exports{"test_symbols"}();

