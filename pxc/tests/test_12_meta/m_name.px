import test_common "";

namespace hoge;
import meta;
import io;

function {t} void show_symbol(t x) {
  string s = meta::concat{t, ": ", meta::nsname{foo}};
  io::println(s);
}

function string foo_to_string(foo f) {
  return "foo_str";
}

struct foo {
}

foo x;
show_symbol(x);
