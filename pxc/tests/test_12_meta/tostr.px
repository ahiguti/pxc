
import generic;
import io;

vector{int} a;
a.resize(5);
foreach (size_t k, int& v : a) {
  v = k;
}

struct foo {
  vector{int} a;
  string b;
}

foo h;
h.a = a;
h.b = "hoge";

io::println(generic::to_string(a));
io::println(generic::to_string(h));

