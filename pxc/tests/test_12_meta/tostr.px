namespace tostr;
import test_common "";

import serialize::string;

vector{int} a;
a.resize(5, int());
foreach (size_t k, int mutable v : a) {
  v = k;
}

struct foo {
  vector{int} a;
  string b;
}

foo h;
h.a = a;
h.b = "hoge";

println(serialize::string::to_string(a));
println(serialize::string::to_string(h));

