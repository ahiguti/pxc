namespace tostr;
import common -;

import serialization::string;

vector{int} a;
a.resize(5, int());
foreach (size_t k, int mutable v : a) {
  v = numeric_cast{int, size_t}(k);
}

threaded struct foo {
  vector{int} a;
  string b;
}

foo h;
h.a = a;
h.b = "hoge";

println(serialization::string::to_string(a));
println(serialization::string::to_string(h));

