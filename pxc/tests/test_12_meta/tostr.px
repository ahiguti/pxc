namespace tostr;
import common -;

import string::conversion;

vector{int} a;
a.resize(5, int());
for (size_t k, int mutable v : a) {
  v = numeric_cast{int, size_t}(k);
}

threaded struct foo {
  vector{int} a;
  string b;
}

foo h;
h.a = a;
h.b = "hoge";

println(string::conversion::to_string(a));
println(string::conversion::to_string(h));

