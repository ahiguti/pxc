namespace m_str;
import test_common "";

import meta::builtin m;

function {t} void show_type(t x) {
  string s = m::to_string{t};
  println(s);
}

threaded struct foo {
}

foo x;
show_type(x);
show_type(3);
