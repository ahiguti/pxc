
import meta;
import io;

function {t} void show_type(t x) {
  // string s = meta::concat{"type: ", meta::string{t}};
  string s = meta::concat{"type: (", t, ")"};
  // string s = meta::concat{t, t};
  io::println(s);
}

struct foo {
}

foo x;
show_type(x);
show_type(3);
