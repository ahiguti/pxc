public namespace back2;
import common -;

struct foo {
  public string s;
  public int x;
  public int y;
}

{
  varray{foo} v;
  v.emplace_back("abc", 10, 20);
  v.emplace_back("xyz", 15, 3);
  println(v);
}

