
namespace mtchk;
import common -;

tsvaluetype struct bar {
  int z;
}

mtvaluetype struct foo(bar const& bx) {
  int x;
  int y;
  bar b;
  tiptr{bar} bp = to_tiptr{bar}(bx);
  function int getbz() const { return bp->z; }
}

tsvaluetype struct baz(bar const& bx) {
  int x;
  int y;
  bar b;
  tiptr{bar} bp = to_tiptr{bar}(bx);
  function int getbz() const { return bp->z; }
}

function void t1() {
  mutable f = foo(bar(55));
  println(f.getbz());
  mutable b = baz(bar(53));
  println(b.getbz());
}

t1();

