import common -;

namespace constr;

threaded struct bar {
}

threaded struct foo(int i, string s) {
  int iv = i + 10;
  string sv = "hoge";
}

{
  ptr{foo} f = make_ptr{foo}(foo(0, ""));
  println(f->iv);
  println(f->sv);
}

