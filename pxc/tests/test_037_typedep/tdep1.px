public namespace tdep1;
import common -;

struct foo {
  int x;
  vector{bar} children;
}

struct bar {
  foo v;
}

function void t1() {
  foo v;
  v.x = 100;
  v.children.push_back(bar(v));
  println(v);
}

t1();
