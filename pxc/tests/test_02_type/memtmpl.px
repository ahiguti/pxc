namespace memtmpl;
import common -;
import meta::builtin m;

struct foo {
  function {t} t mul(t x) {
    return x * x;
  }
}

function void t1()
{
  foo f;
  var x = f.mul(3);
  println(x);
}

t1();
