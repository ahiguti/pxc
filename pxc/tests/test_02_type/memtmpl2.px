namespace memtmpl2;
import common "";
import meta::builtin m;

struct foo {
  function {t} t mul(t x) {
    return x * x;
  }
}

function void t1()
{
  foo f;
  // int x = f.mul(3);
  int x = m::local{foo, "mul"}(3); // FIXME
  println(x);
}

t1();

