
namespace ephemeralbyref;
import common -;

function size_t foo(slice{int}& sl)
{
  size_t r = sl.size();
  sl.next();
  return r;
}

function void t1()
{
  vector{int} v;
  v.resize(30, 0);
  v[10] = 5;
  slice{int} sl = v;
  size_t len = foo(sl);
  println(len);
}

t1();
