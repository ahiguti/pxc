public namespace hex;
public import common -;
public import text::string::positional -;

{
  for (uint i : 0 .. 100U) {
    string s;
    integral_to_hexadecimal{uint}(i, s);
    println(i, s);
  }
  for (int i : -100 .. 100) {
    string s;
    integral_to_hexadecimal{int}(i, s);
    println(i, s);
  }
}
