
namespace test1;
import perl;

perl::export_all{"test1"}();

function perl::sv f1(perl::sv x)
{
  return x;
}

function perl::sv f2(perl::sv x, perl::sv y)
{
  return perl::int_to_sv(perl::sv_to_int(x) + perl::sv_to_int(y));
}

function int f3(int x, int y)
{
  return x + y;
}

struct hoge {
  int x;
  long y;
}

function ref{hoge} f4()
{
  ref{hoge} v = hoge();
  return v;
}

function int f5(ref{hoge} v)
{
  v->x += 1;
  return v->x;
}

