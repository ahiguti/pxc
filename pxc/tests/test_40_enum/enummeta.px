namespace enummeta;
import common -;

enum foo {
  foo1 = 1,
  foo2 = 2,
}

function void test1() {
  foo f = foo1;
  foreach (name, sym : meta::builtin::values{foo}) {
    println(name);
  }
}

test1();

