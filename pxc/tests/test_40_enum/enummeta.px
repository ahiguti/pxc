namespace enummeta;
import common -;
import meta::common m;

enum foo {
  foo1 = 1,
  foo2 = 2,
}

function void test1() {
  foo f = foo1;
  macro vals m::values{foo};
  expand (i : m::seq{m::size{vals}}) {
    println(m::full_string{m::at{vals, i}});
  }
}

test1();

