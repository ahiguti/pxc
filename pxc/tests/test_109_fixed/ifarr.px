public namespace ifarr;
import common -;

{
  varray{int} v;
  v.resize(100, 23);
  if (const m : v[5]) {
    println(m);
  } else {
    println("notfound");
  }
  if (const m : v[111]) {
    println(m);
  } else {
    println("notfound");
  }
}
