public namespace tf1;
import common -;

threaded function int foo(int x, int y)
{
  return x + y;
}

threaded function string bar(string const& x, string const& y)
{
  mutable r = x;
  r.append(y);
  return r;
}

threaded function unit baz(int x)
{
  return unit();
}

{
  println(make_thread_func{foo}(10, 20).__call());
  println(make_thread_func{bar}("abc", "def").__call());
  println(make_thread_func{baz}(3).__call());
}

