public namespace rp1;
import common -;

{
  string buf = "abc";
  strref r = buf;
  rawptr{uchar} p = r.rawptr();
  vector{rawptr{uchar}} v;
  v.push_back(p);
  println(v.size());
  // println(v[0].to_size_t());
}

