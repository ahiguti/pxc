public namespace range1;
public import common -;
public import generic::wellordered -;

{
  int v;
  range_foreach(1, 10, 2, 5) function void(int x, int y) {
    println(x, y);
    v += x * y;
  }
  println(v);
}

