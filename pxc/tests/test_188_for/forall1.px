public namespace forall1;
public import core::common -;
public import core::generic::finite -;

{
  uint v;
  values_foreach() function void(uchar x, uchar y) {
    // println(x, y);
    uint x1 = x;
    uint y1 = y;
    v += x1 * y1;
  }
  println(v);
}
