public namespace range2;
public import core::common -;
public import core::generic::finite -;

{
  int v;
  range_inclusive_foreach(1, 10, 2, 5) function void(int x, int y) {
    println(x, y);
    v += x * y;
  }
  println(v);
}

