namespace use;
public import common -;
public import cre -;

{
  const x = create_foo();
  const v = x->m1(10, 20);
  const s = x->m2("abc", "xyz");
  println(v);
  println(s);
}

// note that pimpl idiom works.
// when use.px is modified and re-compiled, nspri.px is not loaded

