public namespace fe_arr;
import common -;

{
  vector{string} v;
  v.push_back("abc");
  v.push_back("xyz");
  for (size_t x, string y: v) {
    println(x, y);
  }
  for (const x, const y: v) {
    println(x, y);
  }
}

