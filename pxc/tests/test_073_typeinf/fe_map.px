namespace fe_map;
import common -;

{
  tree_map{int, string} v;
  v[5] = "abc";
  v[12] = "xyz";
  for (int x, string y: v) {
    println(x, y);
  }
  for (const x, const y: v) {
    println(x, y);
  }
}

