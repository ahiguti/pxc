#!/usr/bin/pxc

import io;
macro argv io::argv;
macro println io::println;

function {t} t fib(t x) {
  return x <= 1 ? x : fib(x - 2) + fib(x - 1);
}

int x = argv.size() > 0 ? string_to_int(argv[0]) : 10;
int n = argv.size() > 1 ? string_to_int(argv[1]) : 10;
for (int i = 0; i < n; ++i) {
  println(fib(x));
}

