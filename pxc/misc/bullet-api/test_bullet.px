public namespace test_bullet "export-unsafe";
public import common -;
public import bullet_base -;
public import bullet_api -;

{
  const cinfo = btDefaultCollisionConstructionInfo();
  const conf = make_ptr{btDefaultCollisionConfiguration}(cinfo);
  const dispatcher = make_ptr{btCollisionDispatcher}(conf.raw());
  println("done");
}

