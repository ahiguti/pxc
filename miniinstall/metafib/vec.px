namespace vec;
import common -;

function void t1()
{
  vector{int} v;
  int sum = 0;
  for (size_t loop : 0 .. 100000) {
    v.clear();
    for (int i : 0 .. 10000) {
      v.push_back(int(i));
    }
    sum += v[v.size() - 1];
  }
  println(sum);
}

t1();
